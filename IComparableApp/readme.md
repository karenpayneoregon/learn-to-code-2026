# About


- Use [IComparer](https://learn.microsoft.com/en-us/dotnet/api/system.icomparable?view=net-10.0) when you want multiple sorting strategies.
- Includes assertions for `debugging`.
  - `accounts.json` is created if it doesn't exist, and is used to store account information. 