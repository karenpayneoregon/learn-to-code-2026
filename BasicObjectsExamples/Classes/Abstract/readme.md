# About

An [abstract class](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/abstract) is a special type of class that serves as a blueprint for other classes and cannot be instantiated on its own. It is designed to be inherited by derived (non-abstract) classes, which must then provide implementations for any abstract members declared in the base class.